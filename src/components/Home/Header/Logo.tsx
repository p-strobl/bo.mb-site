import styled from "styled-components";

const LogoWrapper = styled.div`
  align-items: center;
  display: flex;
  height: 100%;
`;

export default function Logo() {
  return (
    <LogoWrapper>
      <div>Logo</div>
    </LogoWrapper>
  );
}
